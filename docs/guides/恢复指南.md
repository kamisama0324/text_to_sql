# 项目恢复指南

## 🔧 当前状态
项目结构完整，但缺少 `gradle-wrapper.jar` 文件。

## 📋 恢复步骤

### 方法1：使用现有的Gradle安装
如果系统已安装Gradle：
```bash
cd /Users/zhouxinping/IdeaProjects/practise/springAI-MCP
gradle wrapper
```

### 方法2：手动下载wrapper文件
```bash
cd /Users/zhouxinping/IdeaProjects/practise/springAI-MCP
curl -L -o gradle/wrapper/gradle-wrapper.jar \
  https://services.gradle.org/distributions/gradle-8.5-wrapper.jar
```

### 方法3：重新生成项目（推荐）
1. 访问 https://start.spring.io/
2. 设置参数：
   - Project: Gradle - Groovy
   - Language: Java  
   - Spring Boot: 3.4.0
   - Java: 21 (更稳定) 或 24
   - Group: com.kami
   - Artifact: spring-ai-mcp
   - Dependencies: Spring Web, Spring WebFlux

3. 下载并解压，复制 `gradle/wrapper/gradle-wrapper.jar` 到当前项目

## 🚀 启动项目

完成恢复后：
```bash
./gradlew bootRun --args='--enable-preview'
```

## 📝 项目特性
- ✅ Java 24特性 (虚拟线程、结构化并发)
- ✅ Spring AI MCP集成
- ✅ RESTful API接口
- ✅ 完整项目结构

所有源代码文件都是完整的，只需要恢复构建环境即可正常运行！