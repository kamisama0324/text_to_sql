# 环境配置文件示例
# 复制此文件为 .env 并填写实际的配置值
# 或者在运行 docker-compose 时通过命令行传入

# MySQL配置（可选，有默认值）
# MYSQL_ROOT_PASSWORD=root123456
# MYSQL_DATABASE=test
# MYSQL_USER=springai
# MYSQL_PASSWORD=springai123

# DeepSeek API密钥（必需，启动时需要提供）
# 使用方式: export DEEPSEEK_API_KEY=your_key 或 DEEPSEEK_API_KEY=your_key docker-compose up
DEEPSEEK_API_KEY=

# GitHub Token（可选，仅在使用GitHub MCP时需要）
# GITHUB_TOKEN=

# 应用配置（可选）
# SPRING_PROFILES_ACTIVE=docker

# JVM内存配置（可选）
# JAVA_OPTS=-Xmx1g -Xms512m